#include <stdio.h>
#include "main.h"

/**
  * main - check the code
  * Description: This is done by change of subject
  * @: a is a pointer for swapping *b
  * @: b is a pointer for swapping *a
  * @swap_int: is used to swap the integers
  * Return: Always 0.
  */
void swap_int(int *a, int *b)
{
	*a = *b;
	*b = c;
	*c = *a;
}
