#include "main.h"

/**
  * main - Entry point
  * Description: 'Prints alphabets in lowercase'
  *
 */
void print_alphabet(void)
{
	int a,b;
	for(a = 97; a <=122; a++)
	{
		for(b = 0; b <= 10, b++);
		{
			_putchar(a);
		}
		_putchar('\n');
	}
}
